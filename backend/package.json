{
  "name": "scavvy-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Scavvy Node.js API for the Expo app. Partner-owned — swap mocks for live AI when ready.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "2.8.5",
    "dotenv": "16.6.1",
    "express": "4.21.2"
  }
}

# Scavvy — Expo app

Standalone Expo Router app. No backend required.

```bash
cd frontend
npm install
npx expo start
```

Then press `i` (iOS), `a` (Android), or `w` (web). Or scan the QR code with Expo Go.

Quests, validation, hints, and voice all run locally with mock data and bundled audio clips.
